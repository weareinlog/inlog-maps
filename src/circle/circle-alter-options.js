export default class CircleAlterOptions {
    constructor(weight, fillOpacity, fillColor, color, opacity) {
        this.weight = weight;
        this.fillOpacity = fillOpacity;
        this.fillColor = fillColor;
        this.color = color;
        this.opacity = opacity;
    }
}
